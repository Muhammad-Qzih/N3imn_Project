import 'package:flutter/material.dart';

class CsutomAuthButton extends StatelessWidget {
  const CsutomAuthButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}